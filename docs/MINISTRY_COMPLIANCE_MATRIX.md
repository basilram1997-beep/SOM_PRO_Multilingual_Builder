# Ministry Compliance Matrix

Review date: 2026-08-12

Repository commit reviewed: `0c44347865c83bf8c4450879d73dc55c7455b00b`

SOM PRO version: `0.9.0-rc.1` from root `package.json`

Assessment type: Technical Compliance Assessment. This is not legal advice and is not an official Ministry certification.

Evidence distinction: repository controls, tests, DAST/ZAP, staging evidence, and pentest handoff support security readiness evidence. Formal Ministry supplier compliance requires official Ministry compliance evidence: archived source documents from `sapakim.education.gov.il` or another approved Ministry source, SHA-256 hashes, exact versions/dates, mapped control IDs, and owner approval in `docs/MINISTRY_OFFICIAL_STANDARDS_INTAKE.md`.

Scope reviewed: repository documentation, backend, frontend, Electron desktop app, license server, Prisma schema/migrations, scripts, CI, Docker/deployment files, tests, backup/restore scripts, release/update scripts, and operational documentation.

Official standards reviewed or searched:

- `sapakim.education.gov.il` supplier standards requested by the review brief: official documents were not retrievable from the current environment. Each unverified supplier-standard requirement is marked `Needs Verification`; controlled intake register and archive rules are tracked in `docs/MINISTRY_OFFICIAL_STANDARDS_INTAKE.md` and `docs/official-ministry-standards/README.md`.
- Ministry pedagogical portal page on ICT program, including pedagogical management software, approved content suppliers, cloud environments, and secure organizational email: https://pop-charedi.education.gov.il/enrichment-programs/ict-program/
- Ministry pedagogical portal page on distance learning and digital environments, including approved digital environments and unified identification references: https://pop.education.gov.il/sherutey-tiksuv-bachinuch/distance-teaching-learning-digital/
- Ministry pedagogical portal Q&A on distance learning security and unified identification for Zoom: https://pop.education.gov.il/sherutey-tiksuv-bachinuch/teachers-reported/distance-learning-questions/

Standards that still need official verification from `sapakim.education.gov.il`:

- Information Security Standard for Ministry of Education Suppliers.
- Privacy Procedure for Technological Products, including exact version/date.
- Pedagogical Management Software Standard.
- Technology Compatibility Standard.
- Scheduling and Teaching Staff Management Software Standard.
- Any official Ministry interface/data-exchange specification applicable to pedagogical management software.

## Compliance Matrix

| ID | Standard | Official Requirement | Applicability | Status | Evidence | Tests | Gap | Risk | Recommended Action |
| -- | -------- | -------------------- | ------------- | ------ | -------- | ----- | --- | ---- | ------------------ |
| M-001 | Ministry supplier information security | Exact supplier control set could not be verified from official supplier portal. Needs Official Verification. | High | Needs Verification | Search attempted against `sapakim.education.gov.il`; official intake register: `docs/MINISTRY_OFFICIAL_STANDARDS_INTAKE.md`; archive rules: `docs/official-ministry-standards/README.md`; app security readiness evidence in `apps/backend/src/app.ts:35-64`, `apps/backend/src/middleware/auth.ts:34-88`, `apps/backend/src/middleware/requestProtections.ts:353-456` | `apps/backend/src/middleware/auth.test.ts`, `apps/backend/src/middleware/requestProtections.test.ts`, `apps/backend/src/services/apiRouteInventory.security.test.ts`, `apps/backend/src/services/ministryOfficialStandardsIntake.security.test.ts` | Official control language/version missing; no row is `Mapped` or `Approved` in the intake register. | Ministry review may reject unverifiable mapping. | Obtain official supplier PDF/version, archive with SHA-256, then remap every control. |
| M-002 | Ministry privacy procedure | Exact privacy procedure could not be verified from official supplier portal. Needs Official Verification. | High | Partial | Existing privacy docs: `docs/PRIVACY_REQUIREMENTS.md`, `docs/RETENTION_AND_DELETION_POLICY.md`, `docs/DPA_TEMPLATE_AR_EN_HE.md`; data model in `apps/backend/prisma/schema.prisma:379-590`; lifecycle controls in `apps/backend/src/modules/schools/schools.routes.ts` | `apps/backend/src/services/privacyLifecycleAndExport.security.test.ts` | Export/anonymize/delete behavior and secret exclusion now have runtime evidence, but official requirement mapping and legal retention periods are not verified. | Legal/privacy acceptance risk. | Obtain official procedure, validate retention periods, and map lifecycle controls to exact Ministry clauses. |
| M-003 | Pedagogical management software | Ministry page describes pedagogical management software as handling students, teaching staff, schedules, grades, attendance, discipline/evaluation, approvals/certificates. | High | Partial | Implemented modules: `apps/backend/src/modules/students/students.routes.ts:1-260`, `apps/backend/src/modules/teachers/teachers.routes.ts:1-220`, `apps/backend/src/modules/schedules/schedules.routes.ts`, `apps/backend/src/modules/daily/daily.routes.ts`, `apps/backend/src/modules/reports/reports.routes.ts` | Playwright flows under `tests/e2e/playwright/*attendance*`, `*grade*`, `*reports*`, `*student-management*` | Not mapped to official supplier standard fields or Ministry data exchange. | Functional approval risk. | Map each official pedagogical field/interface once the standard is obtained. |
| M-004 | Technology compatibility | Exact Technology Compatibility Standard and version could not be verified from official supplier portal. | High | Needs Verification | Browser/device tests exist: `.github/workflows/ci.yml:47-100`, `tests/e2e/playwright/compatibility-matrix.spec.js`, `tests/e2e/playwright/mobile-accessibility.spec.js` | CI compatibility matrix for Chromium/Firefox/WebKit; mobile a11y smoke | Official standard version and criteria missing. | Compatibility claims may be challenged. | Obtain standard, then convert it into browser/device/accessibility acceptance criteria. |
| M-005 | Scheduling and teaching staff management | Exact scheduling standard could not be verified from official supplier portal. | High | Needs Verification | Scheduling modules: `apps/backend/src/modules/schedules/schedules.routes.ts`, `apps/backend/src/services/scheduleBuilder.ts`, `apps/backend/src/services/substitutionEngine.ts`, `apps/backend/prisma/schema.prisma:769-893` | `apps/backend/src/services/scheduleRules.test.ts`, `scheduleWorkflow.test.ts`, Playwright daily/schedule flows | No official field/interface mapping. | Scheduling approval risk. | Obtain standard and map periods, staff availability, substitutions, homerooms, conflicts, reports. |
| SEC-001 | Authentication | Secure user authentication. | High | Partial | Login, logout, password change, rate limits: `apps/backend/src/modules/auth/auth.routes.ts:140-320`; token creation/verification: `apps/backend/src/services/authService.ts:41-124` | `apps/backend/src/services/authService.test.ts`, `apps/backend/src/middleware/auth.test.ts` | Password min length is 6; no MFA enforcement; no SSO. | Account compromise risk. | Raise password policy, add MFA enforcement for privileged users, add SSO/IDM path. |
| SEC-002 | Token/session handling | Tokens must expire and be revocable. | High | Partial | Token TTL and HMAC: `apps/backend/src/services/authService.ts:22-124`; tokenVersion and inactivity checks: `apps/backend/src/middleware/auth.ts:34-57`; logout increments tokenVersion: `apps/backend/src/modules/auth/auth.routes.ts:247-252` | `apps/backend/src/services/authService.test.ts:25-75`, `apps/backend/src/middleware/auth.test.ts:62-110` | No refresh-token rotation model; bearer token is custom, not standard JWT/OIDC. | Session management review risk. | Keep revocation tests; consider standard OIDC/JWT libraries for Ministry SSO integration. |
| SEC-003 | Brute-force protection | Login and sensitive actions should be rate limited. | High | Partial | Login rate limit: `apps/backend/src/modules/auth/auth.routes.ts:24-31`; sensitive route limits: `apps/backend/src/middleware/requestProtections.ts:55-249`; Redis optional backing: `apps/backend/src/middleware/requestProtections.ts:30-33` | `apps/backend/src/middleware/requestProtections.test.ts` | Account lockout is not persisted per account; memory fallback is per-process. | Distributed attack may bypass single-process limits. | Require Redis rate-limit backing in production and add per-account lockout policy. |
| SEC-004 | MFA | MFA must be real, not only documented. | High | Partial | TOTP/recovery-code service with encrypted state: `apps/backend/src/services/mfaService.ts`; login enforcement and MFA setup/enable/disable/readiness endpoints: `apps/backend/src/modules/auth/auth.routes.ts`; schema columns: `apps/backend/prisma/schema.prisma:185-187` | `apps/backend/src/services/mfaAndSso.security.test.ts` | Backend TOTP exists for privileged users, but frontend enrollment UX, recovery-code ceremony, device reset process, and production rollout policy still need completion. | Privileged account risk until enforced operationally for all schools. | Add frontend enrollment, admin rollout workflow, WebAuthn/IdP MFA decision, and production gate using `/api/auth/mfa/readiness`. |
| SEC-005 | SSO/Ministry identity | Ministry/approved identity integration if required. | High | Partial | OIDC placeholder endpoints fail closed and reject client-supplied `schoolId`/`role`: `apps/backend/src/modules/auth/auth.routes.ts`; architecture docs: `docs/IDM_SESSION_AND_RBAC_AR.md` | `apps/backend/src/services/mfaAndSso.security.test.ts` | No real IdP metadata, JWKS validation, claim mapping, logout, or Ministry IdM integration yet. | Ministry onboarding may require unified identification/IdM. | Confirm Ministry IdM/OIDC/SAML requirements, then implement trusted claim mapping for user, school, role, and MFA assurance. |
| AUTHZ-001 | Backend RBAC | Backend must enforce role permissions. | High | Partial | App-level RBAC mounting: `apps/backend/src/app.ts:76-99`; permissions table: `apps/backend/src/services/accessPolicy.ts:1-22` | `apps/backend/src/services/accessPolicy.test.ts`, `tests/e2e/playwright/permissions-access.spec.js` | Static role policy; dynamic Role/Permission tables in schema are not wired into enforcement. | Authorization mismatch risk. | Either wire DB roles or document static RBAC as canonical; add route inventory tests. |
| AUTHZ-002 | Object-level authorization | Prevent IDOR/BOLA through server-side school/resource checks. | High | Partial | `schoolId` derived from authenticated request: `apps/backend/src/services/schoolContext.ts:32-44`; body override rejected: `apps/backend/src/middleware/requestProtections.ts:353-390`; tenant-isolation runtime suite covers negative param/query/body IDs across students, attendance, classes, teachers, teacher assignments, subjects, schedules, daily schedules/events, archive, reports, operations metadata, uploads, audit logs, and school lifecycle endpoints: `apps/backend/src/services/tenantIsolation.security.test.ts` | `apps/backend/src/services/apiContracts.test.ts`, `apps/backend/src/services/attendanceIntegrity.test.ts`, `apps/backend/src/services/tenantIsolation.security.test.ts` | Expanded tenant-isolation runtime suite exists, but it is not yet exhaustive across every route/resource type and export/download path. | Cross-school data exposure. | Continue route-by-route negative cross-school tests for every remaining route param/query/body ID. |
| TEN-001 | Tenant isolation assessment | Multi-school data must stay isolated. | High | Partial | Most Prisma models include `schoolId` and indexes: `apps/backend/prisma/schema.prisma:379-1146`; app rejects `schoolId` client overrides: `apps/backend/src/middleware/requestProtections.ts:353-390`; expanded runtime attacks cover students, attendance, classes, teachers, teacher assignments, subjects, base schedules, daily schedules/events, archive, reports, report export records, backup job metadata, uploads, audit logs, and school lifecycle endpoints: `apps/backend/src/services/tenantIsolation.security.test.ts` | `apps/backend/src/services/tenantIsolation.security.test.ts`, contract/integrity tests | Baseline is now broad across core operational objects, but still not comprehensive for all grades, certificates, imports, notifications, report/export download paths, backup files, and every route ID. | P0 if any uncovered route misses `schoolId`. | Continue expanding the suite route-by-route before Ministry pilot. |
| TEN-002 | Tenant source of truth | `schoolId` should come from authenticated server-side context. | High | Partial | `req.user.schoolId` set after token verification: `apps/backend/src/middleware/auth.ts:72-81`; `getRequestSchoolId` uses `req.user.schoolId`: `apps/backend/src/services/schoolContext.ts:32-44` | `apps/backend/src/middleware/auth.test.ts` | Development fallback creates/defaults a school outside production. | Misuse of fallback in production would be severe. | Add tests asserting production routes fail closed without authenticated school context. |
| API-001 | API security baseline | Auth, authz, license, validation, rate limits, CORS, headers. | High | Partial | Helmet/CORS/json limit and safe malformed/oversized JSON errors: `apps/backend/src/app.ts:35-123`; auth/license/audit/protection chain: `apps/backend/src/app.ts:76-82`; generated route inventory: `docs/API_ROUTE_INVENTORY.md`; generator/report: `scripts/runtime/api-route-inventory.js`, `reports/security/api-route-inventory.json`; Zod validation examples: `apps/backend/src/modules/auth/auth.routes.ts:80-116`; live DAST/ZAP gates: `scripts/runtime/dast-baseline.js`, `scripts/runtime/zap-baseline.js`; external pentest handoff/sign-off: `docs/EXTERNAL_PENTEST_HANDOFF_PACK.md`, `docs/EXTERNAL_PENTEST_SIGNOFF_TEMPLATE.md` | `apps/backend/src/services/apiRouteInventory.security.test.ts`, `apps/backend/src/services/apiNegativeValidation.security.test.ts`, `apps/backend/src/services/liveDastPentestEvidence.security.test.ts`, `apps/backend/src/services/externalPentestHandoff.security.test.ts`, `apps/backend/src/services/apiContracts.test.ts`, route unit tests | Source-generated route inventory and dynamic negative tests now cover mass assignment, privilege escalation, malformed/oversized JSON, sensitive ID context override, and error leakage; ZAP baseline gate and external pentest sign-off workflow exist but still need a real staging run and signed tester report. | API exploitation risk if route changes bypass inventory or live DAST/external pentest evidence is not reviewed. | Keep inventory and negative tests in CI, expand route-driven fuzzing, run ZAP/API DAST against staging, execute external pentest handoff, and archive signed reports per release. |
| API-002 | Security headers/TLS | Production must enforce HTTPS and security headers. | High | Partial | Helmet/HSTS enabled in production: `apps/backend/src/app.ts:35-43`; HTTPS middleware: `apps/backend/src/middleware/https.ts`; Nginx now has 80-to-443 redirect, TLS 443 block, HSTS/security headers, Docker resolver, and HTTPS health endpoint: `deploy/nginx/sompro.conf`; compose exposes 443 and fails closed without explicit public API origin: `docker-compose.production.yml`; staging smoke/evidence gates: `scripts/staging-check.js`, `scripts/runtime/staging-evidence-pack.js`, `docs/STAGING_EVIDENCE_PACK.md`; live staging evidence closure: `docs/LIVE_STAGING_EVIDENCE_CLOSURE.md` | `apps/backend/src/services/httpsStagingEvidence.security.test.ts`; `apps/backend/src/services/stagingEvidencePack.security.test.ts`; `apps/backend/src/services/liveStagingEvidenceClosure.security.test.ts`; `node scripts/staging-check.js`; `npm run security:staging-evidence` | Code/config and evidence-pack generator exist, but a live staging domain TLS certificate report, redirect capture, HSTS response headers, and strict dated smoke output are still required. | Transport security risk remains until live staging/production evidence is attached. | Deploy to real staging domain, run `STAGING_EVIDENCE_STRICT=true` evidence pack plus SSL report, close `docs/LIVE_STAGING_EVIDENCE_CLOSURE.md`, and archive output in `reports/security/` and `docs/PHASE_10_STAGING_VERIFICATION_REPORT.md`. |
| PRIV-001 | Personal data inventory | Know student, teacher, auth, audit, uploads, reports, backups data. | High | Partial | Prisma models: `apps/backend/prisma/schema.prisma:172-205`, `379-590`, `896-919`, `1049-1092`; docs `docs/DATA_MAP.md`, `docs/PRIVACY_REQUIREMENTS.md`; school lifecycle export scrubbing in `apps/backend/src/modules/schools/schools.routes.ts` | `apps/backend/src/services/privacyLifecycleAndExport.security.test.ts` | School lifecycle export is proven secret-free and tenant-scoped; official retention periods and artifact deletion duties still need verification. | Privacy non-compliance. | Keep inventory current, verify official privacy procedure, and extend artifact/file retention evidence. |
| PRIV-002 | Data export/delete | Support export/delete/correction where required. | High | Partial | School export/delete routes referenced in rate limits: `apps/backend/src/middleware/requestProtections.ts:219-236`; export/anonymize/delete lifecycle, encrypted artifact metadata, tombstone purge, and audit retention in `apps/backend/src/modules/schools/schools.routes.ts` | `apps/backend/src/services/privacyLifecycleAndExport.security.test.ts` | Runtime evidence now covers non-admin denial, cross-tenant denial, secret-free export, encrypted `.json.enc` metadata, anonymize PII scrubbing, purge tombstone, and audit retention; live storage/KMS deletion evidence remains. | Data subject/request failure. | Add storage-level artifact deletion/KMS retention evidence and legal approval for exact retention windows. |
| AUD-001 | Audit logging | Logs should include actor, school, action, entity/resource, timestamp and redaction. | High | Partial | AuditLog model: `apps/backend/prisma/schema.prisma:896-919`; centralized redaction in `apps/backend/src/services/auditLog.ts`; HTTP audit actor/result in `apps/backend/src/middleware/auditTrail.ts`; OIDC/MFA security events in `apps/backend/src/modules/auth/auth.routes.ts`; DB append-only migration and evidence doc: `apps/backend/prisma/migrations/20260812143000_audit_append_only_and_lifecycle_evidence_guards/migration.sql`, `docs/DB_TENANT_INTEGRITY_EVIDENCE.md` | `apps/backend/src/services/auditImmutabilityAndRedaction.security.test.ts`, `apps/backend/src/services/dbTenantIntegrity.security.test.ts`, `apps/backend/src/services/unitHelpers.test.ts`, `importAndLogIntegration.test.ts` | Backend redaction, API immutability, and DB trigger contract evidence exist, but live staging trigger output, WORM/SIEM retention, and signed export evidence are not complete. | Investigation gaps if infrastructure logging is mutable. | Apply migration on staging, archive failed `AuditLog` update/delete proof, then add WORM/SIEM sink and signed periodic exports. |
| AUD-002 | Audit log modification protection | Ordinary users must not edit/delete audit logs. | High | Partial | Audit logs mounted with `manageSettings`: `apps/backend/src/app.ts:94`; no update/delete audit API; archive deletion no longer deletes audit snapshots: `apps/backend/src/modules/archive/archive.routes.ts`; DB append-only trigger migration: `apps/backend/prisma/migrations/20260812143000_audit_append_only_and_lifecycle_evidence_guards/migration.sql` | `apps/backend/src/services/auditImmutabilityAndRedaction.security.test.ts`, `apps/backend/src/services/dbTenantIntegrity.security.test.ts`, `tests/e2e/playwright/permissions-access.spec.js` | API-level immutability and DB migration contract are proven; database role separation and live trigger evidence remain. | Tampering risk by DB/admin infrastructure access. | Use least-privilege DB runtime role, apply append-only trigger in staging/prod, and archive live trigger test output. |
| BDR-001 | Backup creation | Backups must cover DB and license data. | High | Partial | Encrypted DB/license backup scripts and manifest helper: `deploy/scripts/backup-postgres.sh`, `deploy/scripts/backup-license-data.sh`, `deploy/scripts/backup-crypto.sh`; encrypted product backup service: `apps/backend/src/services/productBackup.ts`; BackupJob model: `apps/backend/prisma/schema.prisma:1072-1092` | `apps/backend/src/services/backupEncryptionAndRpo.security.test.ts`, `apps/backend/src/services/backupRestoreIntegration.test.ts` | Encrypted artifact baseline exists, but production KMS/object-storage retention and signed/offsite evidence are not complete. | Data loss/leakage risk if deployment storage is not hardened. | Add KMS/secret manager, offsite immutable storage, retention enforcement, and signed backup manifests. |
| BDR-002 | Restore/DR | Restore must be tested; RPO/RTO defined. | High | Partial | Restore script decrypts `.enc` backups and blocks plaintext by default: `deploy/scripts/restore-postgres.sh`; RPO/RTO defaults documented: `docs/BUSINESS_CONTINUITY.md`, `docs/STAGING_BACKUP_RESTORE_TEST_AR.md` | `apps/backend/src/services/backupEncryptionAndRpo.security.test.ts`, `apps/backend/src/services/backupRestoreIntegration.test.ts` | RPO/RTO targets are documented in artifacts, but a live staging timed restore report is still required. | Broken restore or unacceptable outage. | Run encrypted restore drill on staging and publish signed RPO/RTO report. |
| SSDLC-001 | Secure software development | CI should include lint, types, tests, dependency scan, secrets, SAST/DAST. | High | Partial | CI quick/unit/e2e/security jobs plus CodeQL SAST, release security artifacts, and Docker-backed OWASP ZAP DAST baseline gate: `.github/workflows/ci.yml`; secrets check: `scripts/security-secrets-check.js`; npm audit wrapper: `scripts/runtime/audit-baseline.js`; local SAST evidence: `scripts/runtime/sast-baseline.js`; DAST gates: `scripts/runtime/dast-baseline.js`, `scripts/runtime/zap-baseline.js`; external pentest handoff: `docs/EXTERNAL_PENTEST_HANDOFF_PACK.md`; sign-off template: `docs/EXTERNAL_PENTEST_SIGNOFF_TEMPLATE.md`; live staging evidence closure: `docs/LIVE_STAGING_EVIDENCE_CLOSURE.md`; staging evidence pack: `scripts/runtime/staging-evidence-pack.js`; Ministry review bundle generator: `scripts/runtime/ministry-review-pack.js`; SBOM/license artifacts: `scripts/generate-sbom.js`, `scripts/license-report.js` | `apps/backend/src/services/securityReleaseEvidence.security.test.ts`; `apps/backend/src/services/stagingEvidencePack.security.test.ts`; `apps/backend/src/services/liveDastPentestEvidence.security.test.ts`; `apps/backend/src/services/externalPentestHandoff.security.test.ts`; `apps/backend/src/services/liveStagingEvidenceClosure.security.test.ts`; `apps/backend/src/services/ministryReviewPack.security.test.ts`; `npm run security:release-evidence`; `npm run security:dast`; `npm run security:staging-evidence`; `npm run security:pentest:prep`; `npm run ministry:review-pack` | CodeQL/SBOM/license/SAST artifact baseline, HTTPS DAST smoke, ZAP evidence gate, evidence-pack generator, live staging evidence closure workflow, Ministry review bundle generator, and external pentest handoff/sign-off workflow exist, but live DAST/strict staging evidence and signed external pentest report are still missing. | Supply chain/code security gaps remain if release artifacts are not reviewed or staging evidence is not run in strict mode. | Archive CI artifacts for each release, configure `STAGING_URL`, run strict staging evidence pack and ZAP DAST, generate `reports/ministry-review/manifest.json`, complete `docs/LIVE_STAGING_EVIDENCE_CLOSURE.md`, complete external pentest sign-off, then attach signed evidence. |
| SUP-001 | Dependency/supply chain | Lockfiles, dependency scanning, provenance. | Medium | Partial | `package-lock.json`; `allowScripts` in `package.json`; `npm audit --omit=dev` wrapper: `scripts/runtime/audit-baseline.js`; CycloneDX SBOM generator: `scripts/generate-sbom.js`; dependency license report: `scripts/license-report.js`; release artifact upload in `.github/workflows/ci.yml` | `npm run security:baseline`; `apps/backend/src/services/securityReleaseEvidence.security.test.ts` | SBOM/license artifacts now exist, but release provenance/signing, dependency review approvals, and fail-closed network audit in hosted CI still need final policy. | Vulnerable dependency or untrusted build risk. | Add signed release provenance, dependency-review policy, and release sign-off over generated SBOM/license/audit artifacts. |
| ELEC-001 | Electron hardening | nodeIntegration off, contextIsolation on, sandbox/preload/IPC/nav restrictions. | High | Partial | Hardened BrowserWindow preferences with `contextIsolation`, `nodeIntegration: false`, `sandbox`, `webSecurity`, and mixed-content blocking: `apps/desktop/src/window.js`; navigation and external URL allowlist policy: `apps/desktop/src/securityPolicy.js`; sandbox-compatible preload IPC bridge: `apps/desktop/preload.js` | `apps/desktop/security.test.js`; `npm run desktop:check` | Desktop shell hardening is now proven by static/policy tests, but packaged installer runtime verification, code-signing, and update integrity evidence are still pending. | Desktop compromise/phishing/data exposure risk if packaged runtime/signing is not verified. | Run packaged desktop security check, complete signing/update verification, and archive screenshots/logs for release evidence. |
| ELEC-002 | Desktop signing/update security | Installer signing and update integrity. | High | Partial | Signing env gate and signed build script: `apps/desktop/electron-builder.config.cjs`, `apps/desktop/package.json`; installer hash/AuthentiCode evidence gate: `scripts/desktop-signing-check.js`; no auto-update channel is enabled, so update integrity is currently manual signed-installer plus SHA-256 evidence | `apps/desktop/security.test.js`; `npm run desktop:signing:check`; `npm run desktop:check` | Baseline gate exists, but real code-signing certificate, signed installer artifact, and final Authenticode verification report are still required. | Installer trust/update tampering risk until signed release evidence is attached. | Acquire signing certificate, run signed build with `SOM_ENABLE_CODESIGN=true`, verify installer signature/hash, and archive `reports/security/desktop-signing-report.json`. |
| LIC-001 | License server boundary | Auth/admin authorization, signing, activation, communication security. | High | Partial | HMAC signing/parsing: `apps/license-server/src/server.js:81-104`; admin bearer token and rate limit: `apps/license-server/src/server.js`; one-time reset-token recovery and security events: `apps/license-server/src/server.js`; activation/status nonce replay protection: `apps/license-server/src/server.js` | `apps/license-server/src/server.security.test.js`, `apps/backend/src/services/licenseService.test.ts`, `licensePolicy.test.ts` | Recovery no longer returns plaintext passwords and client nonce replay protection is enforced in strict mode; local JSON storage, DB/KMS migration, live HTTPS evidence, and operational reset-token delivery policy remain. | License tampering or credential/reset-token exposure if storage or operational handoff is weak. | Move storage to DB/KMS, enable strict client nonce policy in staging/production, add signed operational reset-token delivery, and archive live HTTPS/license-server evidence. |
| LIC-002 | License server availability | Behavior if license server unavailable; SPOF analysis. | High | Partial | Backend license guard calls `getLicenseState`: `apps/backend/src/middleware/licenseGuard.ts:8-25`; docker healthcheck: `docker-compose.production.yml:55-69` | `scripts/e2e-license.js`, `stress:outage` | Central license server outage behavior needs full staging test; local read-only/fail behavior must be documented to schools. | License server may be single point of failure for activation/recovery/status. | Define grace-period/fail-open-read-only policy and test outage scenarios. |
| UPLOAD-001 | File upload security | Validate type/size/name, scan, tenant-isolate storage, block dangerous content. | High | Partial | Raw upload only, size/type checks, random name, per-school path: `apps/backend/src/modules/uploads/uploads.routes.ts:18-120`; scanning call: `apps/backend/src/services/fileUploadScanning.ts` | `apps/backend/src/services/fileUploadScanning.test.ts`, `uploadRoute.test.ts` | MIME header is client-provided; no public download policy reviewed; malware scanner may be stub/policy-based. | Malware/path/content exposure. | Verify scanner backend, sniff magic bytes, add download auth and content-disposition tests. |
| DB-001 | Database tenant integrity | Models containing school data need ownership, indexes, constraints. | High | Partial | School-scoped models and compound uniques: `apps/backend/prisma/schema.prisma:172-1146`; export/backup restrict FKs and audit trigger migration: `apps/backend/prisma/migrations/20260812143000_audit_append_only_and_lifecycle_evidence_guards/migration.sql`; evidence report: `docs/DB_TENANT_INTEGRITY_EVIDENCE.md` | `apps/backend/src/services/databaseIntegrity.test.ts`, `apps/backend/src/services/dbTenantIntegrity.security.test.ts`, `attendanceIntegrity.test.ts` | Model-by-model school ownership and lifecycle evidence guardrails are now tested; live DB role separation and staging migration proof still needed. | Cross-school data, orphaned data, accidental deletion. | Apply migration to staging, archive trigger/FK proof, and add ERD plus least-privilege DB role evidence. |
| DB-002 | Transactions/concurrency | Avoid partial writes/duplicates. | Medium | Partial | Upserts/compound unique keys for attendance/grades/certificates: `apps/backend/src/modules/students/students.routes.ts:201-236`, `apps/backend/prisma/schema.prisma:435-590` | `attendanceIntegrity.test.ts`, `workflowIntegration.test.ts` | Not all multi-step operations are proven transactional. | Partial updates under concurrent use. | Add concurrency tests for transfers, grade entry, attendance storms, backup during writes. |
| ACC-001 | Accessibility | Keyboard, semantics, labels, focus, contrast, zoom, RTL. | High | Partial | Frontend accessibility docs/tests: `docs/ACCESSIBILITY_READINESS_AR.md`, `tests/e2e/playwright/mobile-accessibility.spec.js`; layout focus logic: `apps/frontend/src/app/main.tsx` | Playwright mobile accessibility smoke | Manual screen-reader/contrast/browser matrix not complete. | Accessibility rejection or unusable flows. | Run manual WCAG/Israeli accessibility review and add axe where possible. |
| RTL-001 | Arabic/Hebrew/RTL | Arabic and Hebrew UI, mixed RTL/numbers, printing, PDFs, exports. | High | Partial | Dictionaries: `apps/frontend/src/i18n/dictionaries/ar.ts`, `he.ts`; locale registry: `apps/frontend/src/i18n/localeRegistry.ts`; print CSS: `apps/frontend/src/styles/print.css` | `apps/frontend/src/i18n/*.test.mjs`, `scripts/i18n-audit.mjs`, Playwright flows | No explicit E2E matrix for Arabic and Hebrew across every core workflow/export. | Target audience usability risk. | Add Arabic/Hebrew E2E and PDF/print visual tests. |
| PERF-001 | Performance/scalability | Ministry-scale use: login storm, attendance, reports, backups, Redis/DB slowdown. | High | Partial | Stress/perf scripts: `scripts/stress-run.js`, `scripts/perf-run.js`, `scripts/scalability-test.js`; CI performance smoke: `.github/workflows/ci.yml:312-386` | `npm run stress:*`, `volume:test`, `perf:*` | Scripts do not prove Ministry-scale deployment; staging load evidence missing. | Pilot slowdown/outage. | Run load test with Ministry pilot assumptions and publish report. |
| REL-001 | Reliability/resilience | Redis/Postgres/license outage, retries, duplicate requests, partial transactions. | High | Partial | Resilience scripts: `scripts/resilience-test.js`, `scripts/runtime/reliability-check.js`; CI chaos smoke: `.github/workflows/ci.yml:203-249` | `npm run chaos:test`, `npm run resilience:test` | Chaos tests are lightweight; no production DR/HA architecture proof. | Availability and data consistency risk. | Add HA design, retry/idempotency policy, and outage drills. |
| INT-001 | Data export and Ministry integration | CSV/JSON/reports are not equivalent to official Ministry integration. | High | Needs Verification | Export/report modules: `apps/backend/src/modules/reports/reports.routes.ts`; frontend export helpers: `apps/frontend/src/features/exports/exportAudit.ts`, `apps/frontend/src/features/archive/archiveExport.ts` | Report/archive Playwright tests | Official Ministry data exchange interfaces not obtained. | Integration approval risk. | Obtain official interface specs and implement conformance tests. |
| OPS-001 | Hosting/provider compliance | Application compliance is separate from provider certifications and data region. | High | Needs Verification | Docker production deployment exists: `docker-compose.production.yml`; hosting docs: `docs/HOSTING_REQUIREMENTS.md`, `docs/HOSTING_PROVIDER_DECISION.md`, `docs/ISRAEL_VPS_STAGING_PROVISIONING.md`; nginx HTTPS placeholder: `deploy/nginx/sompro.conf` | `npm run production:check`, staging check scripts, `apps/backend/src/services/hostingProviderDecision.security.test.ts`, `apps/backend/src/services/israelVpsStagingProvisioning.security.test.ts` | No evidence of final host, Israel data residency contract/statement, ISO/SOC provider reports, DPA, SLA, or KMS. VPS provisioning is a runbook only until a provider is selected and evidence is archived. | Procurement/compliance blocker. | Select Israel-hosted provider, collect provider certificates/security package, document data residency in Israel, DPA, SLA, KMS/secret manager, backup location, then run the Israel VPS staging provisioning checklist if VPS is selected. |
| SEC-006 | Secrets management | Secrets must not be committed or exposed. | High | Partial | `.gitignore`, examples, secrets check: `scripts/security-secrets-check.js`; KMS/rotation readiness: `docs/SECRETS_AND_KMS_READINESS.md`; production placeholders and backup passphrase file reference: `.env.production.example`, `apps/backend/.env.production.example`, `apps/license-server/.env.production.example` | `npm run security:secrets`; `apps/backend/src/services/secretsAndKmsReadiness.security.test.ts` | Local `.env` exists and final provider/KMS is not selected; app-wide `_FILE` support for every secret is not complete. | Secret leakage or unrecoverable rotation during staging/production handoff. | Rotate local/demo secrets before handoff, use managed secret store/KMS or protected deployment env in production, prefer `_FILE`/Docker secret for backup passphrase, and archive masked secret-store evidence. |

## Tenant Isolation Assessment

Result: PARTIAL

`schoolId` is generally derived server-side from authenticated token context in `apps/backend/src/middleware/auth.ts:72-81` and exposed through `getRequestSchoolId` in `apps/backend/src/services/schoolContext.ts:32-44`. The app-level middleware rejects client-supplied `schoolId` and `userId` in write bodies in `apps/backend/src/middleware/requestProtections.ts:353-390`. The Prisma schema has broad school ownership and compound indexes/uniques across core models in `apps/backend/prisma/schema.prisma:379-1146`.

An expanded runtime test now exists in `apps/backend/src/services/tenantIsolation.security.test.ts`. It seeds two schools and proves that a school A admin cannot access or mutate school B data through selected student list/context, attendance read/write, class update, teacher and teacher-assignment update, subject update, base schedule writes/swaps, daily schedule/event access, archive delete, report queries, operations metadata for report exports and backup jobs, upload storage context, audit-log detail/list, school dashboard, school export, or school delete-data endpoints.

The remaining gap is full route inventory coverage. The suite still needs to prove the same negative behavior for every grade/certificate/import/notification/export/download path, backup file access path, report artifact path, and every remaining route/resource ID. This should remain a high-priority Ministry pilot blocker until covered.

## Personal Data Inventory

| Data Category | Models/Tables | Purpose | Who Can Access | Retention Defined? | Encryption | Export/Delete Support |
| ------------- | ------------- | ------- | -------------- | ------------------ | ---------- | --------------------- |
| Student personal information | `Student`, linked `User` | Student management, class placement, portals | Backend roles with `read` plus route-specific teacher/student scope | School export retention metadata defaults to 30 days; exact legal period Needs Verification | DB/storage-level encryption depends on hosting; stored lifecycle artifacts are marked encrypted `.json.enc` | School export/anonymize/delete runtime evidence exists; storage/KMS deletion Needs Verification |
| Teacher information | `Teacher`, `TeacherAssignment`, `DailyTeacherStatus`, `User` | Staff management, schedules, substitutions | Admin/manager/scheduler/teacher-scoped routes | Policy docs exist; exact period Needs Verification | Depends on hosting | Partial |
| Attendance | `StudentAttendance`, `AttendanceRecord` | Daily attendance and certificates | Admin/teachers with class scope; students/parents via portal scope where implemented | Needs Verification | Depends on hosting | Partial |
| Grades/academic records | `StudentGradeEntry`, `StudentAcademicRecord`, `GradeRecord` | Grades, academic evaluation, certificates | Backend role/scope controls | Needs Verification | Depends on hosting | Partial |
| Behavioral information | `StudentBehaviorRecord`, `ClassroomLog` | Behavior/performance notes | Backend role/scope controls | Needs Verification | Depends on hosting | Partial |
| Authentication data | `User.password`, `tokenVersion`, `lastActivityAt`, MFA fields | Login/session security | Backend auth code/admin user management | Needs Verification | Passwords hashed with scrypt; MFA secrets excluded from lifecycle export and cleared on anonymize | Password change/logout plus school lifecycle export/anonymize/delete proof |
| Audit/security data | `AuditLog`, `SecurityIncident` | Investigation, monitoring, incident response | `manageSettings` routes | Needs Verification | Depends on hosting/log sink | Export path exists; immutability Needs Verification |
| Uploaded files | filesystem `tmp/uploads/<schoolId>/...` | School file artifacts/imports | Upload route requires authenticated admin/settings path | Needs Verification | Filesystem encryption depends on host | Export/delete lifecycle not fully proven |
| Certificates/reports | `StudentCertificate`, `ReportExport` | Certificates and reporting | Backend role/scope controls | `expiresAt` on exports; policy Needs Verification | Depends on host/storage | Partial |
| Backups | `BackupJob`, encrypted DB/license artifacts, manifests | Disaster recovery and lifecycle export evidence | Admin/operations | RPO/RTO documented; lifecycle export metadata defaults to 30-day expiry; exact retention Needs Verification | Backup scripts and product backups emit encrypted artifacts; school export snapshot metadata is `.json.enc`; KMS/storage evidence still deployment-dependent | Restore exists; school lifecycle proof retains encrypted evidence metadata; deletion from offsite backups Needs Verification |

## Executive Gap Summary

### P0 - Critical / Block Ministry Pilot

- Tenant-isolation testing now has an expanded runtime baseline across core operational routes, but it is not yet comprehensive across every route, grade/certificate/import/notification/export/download path, backup file path, and resource ID.
- MFA backend enforcement now exists for privileged users, but frontend rollout, reset ceremony, and production policy evidence are not complete.
- Ministry supplier standards from `sapakim.education.gov.il` were not officially verified, so the matrix cannot claim formal Ministry supplier compliance; the required intake register and archive workflow are now documented in `docs/MINISTRY_OFFICIAL_STANDARDS_INTAKE.md`.
- License server is a security boundary; recovery now uses one-time reset tokens and strict nonce replay tests, but local JSON storage and operational reset-token delivery still require DB/KMS and deployment evidence.
- Production HTTPS configuration now has nginx 443, HTTP redirect, HSTS, and static staging verification, but real-domain TLS/staging evidence is still missing.

### P1 - Required Before Production

- Complete real SSO/OIDC/SAML or approved Ministry identity integration once provider details are verified.
- Complete staging deployment on a real domain with TLS, monitoring, backup/restore drill, and outage tests.
- Improve audit log infrastructure evidence: external append-only/WORM sink, signed exports, retention/immutability proof, and SIEM runbook.
- Review and archive CodeQL/SBOM/license/security artifacts for every release; run live HTTPS DAST/ZAP evidence and attach external pentest sign-off.
- Complete packaged Electron signing/update evidence after shell sandbox, preload, and URL allowlists are now covered by tests.
- Complete backup KMS/offsite retention, signed manifests, and live staging RPO/RTO drill evidence.

### P2 - Required for Formal Compliance / Operational Maturity

- Map every official Ministry standard requirement once official PDFs/versions are obtained.
- Build an API route inventory with auth/authz/tenant/rate-limit/validation evidence per route.
- Add Arabic/Hebrew RTL E2E coverage for printing/PDF/export and mixed text/numbers.
- Run manual accessibility review and browser/device matrix.
- Produce provider certification/data-region/KMS evidence separately from application controls.

### P3 - Improvements

- Centralize white-label branding.
- Add broader observability dashboards and alerting evidence.
- Convert policy-only docs into generated evidence packs.

## Remediation

| Priority | Finding | Required Change | Files Likely Affected | Test Required | Dependency/External Requirement |
| -------- | ------- | --------------- | --------------------- | ------------- | ------------------------------- |
| P0 | Tenant isolation not exhaustively proven | Continue cross-school negative tests for every remaining route/resource ID and artifact path using the generated route inventory as the checklist | `apps/backend/src/modules/**`, `apps/backend/src/services/**`, `docs/API_ROUTE_INVENTORY.md`, `apps/backend/src/services/tenantIsolation.security.test.ts` | Automated integration/security tests | None |
| P0 | MFA missing | Implement MFA enrollment/challenge/recovery and admin enforcement | `apps/backend/src/modules/auth/**`, `apps/frontend/src/features/auth/**`, Prisma schema/migrations later | Auth unit/e2e/security tests | Approved MFA method/IdP |
| P0 | Official Ministry standards unavailable | Obtain and map standards exactly | `docs/MINISTRY_COMPLIANCE_MATRIX.md` | Documentation review | `sapakim.education.gov.il` access |
| P0 | API route inventory drift | Keep source-derived auth/RBAC/tenant/rate/audit/upload coverage current for every backend route | `scripts/runtime/api-route-inventory.js`, `docs/API_ROUTE_INVENTORY.md` | `apps/backend/src/services/apiRouteInventory.security.test.ts`, `npm run security:api-inventory` | None |
| P0 | License recovery exposes password | Replace plaintext recovery with one-time reset flow | `apps/license-server/src/server.js`, backend license sync | `apps/license-server/src/server.security.test.js` | Secure mail/admin handoff policy |
| P1 | TLS/staging incomplete | Run and archive real-domain HTTPS staging verification after the config baseline | `deploy/nginx/sompro.conf`, `docker-compose.production.yml`, `scripts/staging-check.js`, `docs/PHASE_10_STAGING_VERIFICATION_REPORT.md` | `apps/backend/src/services/httpsStagingEvidence.security.test.ts`, `node scripts/staging-check.js`, live SSL/staging smoke | Domain/certificates |
| P1 | Audit logging not investigation-grade | Add userId/IP/result/redaction/immutability tests | `apps/backend/src/middleware/auditTrail.ts`, `apps/backend/src/services/auditLog.ts` | Audit security tests | SIEM/WORM decision |
| P1 | Backup encryption/retention not fully evidenced | Add KMS/offsite retention, signed manifests, and live encrypted restore drill | `deploy/scripts/*.sh`, `scripts/runtime/update-manager.js`, `docs/STAGING_BACKUP_RESTORE_TEST_AR.md` | Restore drill test | KMS/secret manager |
| P1 | Electron sandbox/signing gaps | Verify packaged runtime, signing, and update integrity after shell hardening | `apps/desktop/src/window.js`, `apps/desktop/src/securityPolicy.js`, `apps/desktop/preload.js`, `electron-builder.config.cjs` | `apps/desktop/security.test.js`, `npm run desktop:check` | Code-signing certificate |
| P1 | API negative testing breadth | Expand dynamic mass-assignment, validation, malformed payload, IDOR, and error-leak tests using the generated route inventory | `apps/backend/src/services/apiNegativeValidation.security.test.ts`, `docs/API_ROUTE_INVENTORY.md` | Backend security test suite | None |
| P1 | SSDLC scanning incomplete | Review CodeQL/SBOM/license artifacts and run live DAST/ZAP before release | `.github/workflows/ci.yml`, `scripts/runtime/sast-baseline.js`, `scripts/runtime/dast-baseline.js`, `scripts/runtime/zap-baseline.js`, `scripts/generate-sbom.js`, `scripts/license-report.js`, `docs/EXTERNAL_PENTEST_HANDOFF_PACK.md`, `docs/EXTERNAL_PENTEST_SIGNOFF_TEMPLATE.md` | `apps/backend/src/services/securityReleaseEvidence.security.test.ts`, `apps/backend/src/services/liveDastPentestEvidence.security.test.ts`, `apps/backend/src/services/externalPentestHandoff.security.test.ts`, `npm run security:release-evidence`, `npm run security:dast`, `npm run security:pentest:prep` | Real `STAGING_URL`, Docker/ZAP runtime, external pentest reviewer |
| P2 | Ministry integration unknown | Obtain official data-exchange spec and implement conformance tests | Reports/export/API modules | Integration tests | Ministry interface specification |
